		apiVersion: v1
kind: Pod
metadata:
  name: nfspod
spec:
  containers:
    - name: mycont1
      image: nginx
      ports:
        - containerPort: 80
      volumeMounts:
        - name: mydata
          mountPath: /usr/share/nginx/html
  volumes:
    - name: mydata
      nfs:
        server: fs-0afb3d3e269b89e37.efs.ap-south-1.amazonaws.com
        path: /test
